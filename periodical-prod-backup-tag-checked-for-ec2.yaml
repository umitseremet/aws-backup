policies:
  - name: periodical-prod-backup-tag-checked-for-EC2
    resource: ec2
    filters:
     - and:
       - tag:makebackup: absent
       - or:
         - tag:ENV: prod
         - tag:env: prod
    actions:
     - type: tag
       key: makebackup
       value: 30d
